function suggest_improvements
walk_and_cat_source | g "$argv" respond with the code changes you would recommend in the format of a json array with keys being files and the values being the new code file with comments containing the suggestion as well as the code changes

end
